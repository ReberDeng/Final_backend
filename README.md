This repository is used to store the app backend code

This is the backend of a mobile application developed using Fastapi

It mainly contains the following api interfaces

```
/api/dogs/detect
/api/dogs/{id}
/api/articles/{id}
/api/articles
```

